#include <stdio.h>
#define STUDENTS 3
#define EXAMS 4

// function prototypes
int minimum(const int grades[][EXAMS], size_t students, size_t exams);
int maximum(const int grades[][EXAMS], size_t students, size_t exams);
double average(const int setOfGrades[], size_t exams);
void printArray(const int grades[][EXAMS], size_t students, size_t exams);

int main(void)
{
}